# PPT Design Lab 协作规则

## 角色与所有权

- Codex 负责需求澄清、架构、权威实现、测试、版本、项目文档、GitHub发布配置及必要的YYOB控制面更新。
- WorkBuddy 负责公司电脑真实环境的高频验证、记录可复现问题，并在任何可能写入业务数据前检查当前项目入口和隐私边界。
- Cindy 决定产品目标、公司环境允许范围、品牌资产使用和正式发布。

## 单一事实源

- 唯一项目根：`/Users/cindy/Projects/04-职业发展/ppt-design-lab`。
- YYOB `PRJ-012` 仅是项目控制面，不保存产品实现和公司PPT内容。
- 未来GitHub仓库是项目源码的发布镜像；在正式创建并登记前，不将其视为权威根。
- 公司电脑上的PPT、HTML、图片、品牌模板和浏览器本地数据是用户本地资产，不属于项目仓库。

## 不可突破的隐私边界

1. 不把公司业务内容、Logo、模板、字体、人员信息、客户信息或生成结果写入项目、YYOB或GitHub。
2. 不加入分析统计、遥测、云端错误上报、业务后端、远程数据库或隐藏网络传输。
3. 导入内容默认只在浏览器内存处理；本地持久化必须明确标注并只使用本机浏览器存储。
4. 导入HTML中的脚本、事件处理器、iframe、远程资源和网络调用默认禁用或隔离。
5. 不逆向LorealGPT接口，不读取其Cookie或登录状态；AI桥接只通过用户主动复制粘贴。
6. 发布前必须用浏览器网络记录验证：初始工具加载完成后，导入、编辑、保存与导出测试全过程无内容外传。

## 项目结构规则

- `README.md`：目标、边界、真源地图和当前状态。
- `docs/workflow.md`：正常用户工作流和数据流。
- `docs/acceptance.md`：v0.1可观察验收标准。
- `scripts/audit_project.py`：结构与敏感资产边界审计。
- 产品实现目录只在进入开发时按实际需要创建，不预建空目录。
- 不创建重复项目根，不把 `/Users/cindy/codex/Gen Image` 中的既有资产迁入本项目。

## 变更与交接

- 修改项目前重新读取本文件、README及YYOB `PRJ-012`。
- 项目级版本、结构、工作流、边界或生命周期变化时更新YYOB；普通测试和单次PPT制作不更新控制面。
- 若未来形成共享技能，先按项目治理规则确定管理模式和唯一权威源码，再登记技能注册表。
- 未经Cindy确认，不改变项目目标、完成标准、合并、拆分、归档或删除项目。

## 完成检查

1. 运行 `python3 scripts/audit_project.py`。
2. 执行与变更风险相称的产品测试。
3. 检查README、AGENTS、工作流、验收标准、实现与YYOB记录是否需要同步。
4. 若更新YYOB，按锁、重读、revision递增、全量验证、生成Dashboard、释放锁的顺序完成。
5. 只有公司电脑真实浏览器验证通过，才可宣布对应版本正式可用。

